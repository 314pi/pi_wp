Lấy xx (ví dụ: 05) mã đơn hàng mới nhất từ woocommerce wordpress
<?php
	$last_order_id = wc_get_orders(array('limit' => 5, 'return' => 'ids')); // Get last Order ID (array)
	$order = wc_get_order( $last_order_id[0] ); // Get an instance of the WC_Order oject
	$user_id = $order->get_user_id();
	$user = get_user_by( 'id', $user_id);
	if (empty($user->display_name)) {
		echo "Khách";
	} else {
		echo $user->display_name ;
	}
?>
=========================================